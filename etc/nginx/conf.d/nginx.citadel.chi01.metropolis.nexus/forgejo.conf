server {
    listen 443 quic;
    listen 443 ssl;
    listen [::]:443 quic;
    listen [::]:443 ssl;
    listen 8443 ssl proxy_protocol;

    server_name forgejo.metropolis.nexus;

    ssl_certificate /etc/letsencrypt/live/forgejo.metropolis.nexus/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/forgejo.metropolis.nexus/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/forgejo.metropolis.nexus/chain.pem;

    include headers-more.d/universal.conf;
    include snippets/proxy.conf;

    more_clear_headers X-Robots-Tag;

    more_set_headers "Content-Security-Policy: default-src none; connect-src 'self'; img-src 'self'; manifest-src data:; script-src-elem 'self' 'unsafe-inline'; style-src-elem 'self' 'unsafe-inline'; worker-src 'self'; form-action 'none'; frame-ancestors 'none'; upgrade-insecure-requests; base-uri 'none'";

    location / {
        proxy_pass http://192.168.0.121:3000;
    }
    
}
